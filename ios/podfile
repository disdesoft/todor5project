platform :ios, '12.0'
use_frameworks!
use_modular_headers!

target 'Runner' do
  use_frameworks!
  use_modular_headers!

  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
