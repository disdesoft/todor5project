platform :ios, '18.0'
use_frameworks!
use_modular_headers!

target 'Runner' do
  
  pod 'Firebase/Core'
  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Functions' 

  target 'RunnerTests' do
    inherit! :search_paths
  end
end
